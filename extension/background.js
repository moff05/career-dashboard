// Toolbar icon is served from the static PNG files declared in manifest.json
// (icon16.png / icon48.png / icon128.png), which are generated from the same
// SVG source as app/icon.png so all surfaces stay pixel-consistent.
